flask
requests
beautifulsoup4
numpy
gunicorn
stripe
# --- Analizor pariuri fotbal ---
# scipy: folosit de modelul Poisson (optional; daca lipseste, exista o implementare proprie)
scipy
# playwright: extragerea reala din Flashscore (NECESAR pe Render pentru analiza reala)
# Dupa "pip install -r requirements.txt" ruleaza si: python -m playwright install chromium
playwright
